import { TestBed } from '@angular/core/testing';

import { AdmdeactivatedrewardsresolverService } from './admdeactivatedrewardsresolver.service';

describe('AdmdeactivatedrewardsresolverService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: AdmdeactivatedrewardsresolverService = TestBed.get(AdmdeactivatedrewardsresolverService);
    expect(service).toBeTruthy();
  });
});
